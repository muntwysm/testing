<h2>All users</h2>

<p><%= "Total Users: #{@all_users} - (#{@active_users} Active and #{@inactive_users} Inactive) " %></p>
<%= will_paginate %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate %>

